version: 1 
frontend: 
  phases: 
    preBuild: 
      commands: 
        - echo "Production build for prod.profolia.art" 
    build: 
      commands: 
        - echo "Static files ready" 
  artifacts: 
    baseDirectory: / 
    files: 
      - '**/*' 
